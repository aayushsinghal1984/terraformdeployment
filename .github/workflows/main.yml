name: 'Terraform Main Deployment Pipeline'

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
    inputs:
      target_env:
        description: 'Target Environment (sit, uat, production)'
        required: true
        default: 'sit'
        type: choice
        options:
          - sit
          - uat
          - production
      working_dir:
        description: 'Root directory of your Terraform modules'
        required: true
        default: 'terraform'
        type: string

jobs:
  # 1. Run Static Code Analysis (Linting)
  tflint-check:
    name: 'Linting'
    uses: ./.github/workflows/tf-tflint.yml
    with:
      # Yaha env.TARGET_ENV ki jagah direct inputs use kiya hai jo 100% work karega
      target_env: ${{ inputs.target_env || 'sit' }}
      working_dir: ${{ inputs.working_dir || 'terraform/azure' }}
      is_apply: false
      tfc_org: ${{ vars.TF_CLOUD_ORG }}
    secrets: inherit

  # 2. Run Security Vulnerability Scanning
  tfsec-scan:
    name: 'Security Scan'
    needs: tflint-check
    uses: ./.github/workflows/tf-tfSec.yml
    with:
      target_env: ${{ inputs.inputs.target_env || 'sit' }}
      working_dir: ${{ inputs.working_dir || 'terraform/azure' }}
      is_apply: false
      tfc_org: ${{ vars.TF_CLOUD_ORG }}
    secrets: inherit

  # 3. Generate Terraform Plan and Upload Binary Artifact
  terraform-plan:
    name: 'TF Plan'
    needs: tfsec-scan
    uses: ./.github/workflows/tf-init-validate.yml
    with:
      target_env: ${{ inputs.target_env || 'sit' }}
      working_dir: ${{ inputs.working_dir || 'terraform/azure' }}
      is_apply: false
      tfc_org: ${{ vars.TF_CLOUD_ORG }}
    secrets: inherit

  # 4. Open Policy Agent (OPA) Compliance Check
  opa-compliance:
    name: 'OPA Compliance'
    needs: terraform-plan
    uses: ./.github/workflows/tf-opa-check.yml
    with:
      target_env: ${{ inputs.target_env || 'sit' }}
      working_dir: ${{ inputs.working_dir || 'terraform/azure' }}
      is_apply: false
      tfc_org: ${{ vars.TF_CLOUD_ORG }}
    secrets: inherit

  # 5. Manual Approval Gate 
  manual-approval:
    name: 'Approval Gate'
    needs: opa-compliance
    uses: ./.github/workflows/manual-validation.yml

  # 6. Execute Terraform Apply, Tagging and Versioning
  terraform-apply:
    name: 'TF Apply & Tag'
    needs: manual-approval
    uses: ./.github/workflows/tf-apply.yml
    with:
      target_env: ${{ inputs.target_env || 'sit' }}
      working_dir: ${{ inputs.working_dir || 'terraform/azure' }}
      is_apply: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
      tfc_org: ${{ vars.TF_CLOUD_ORG }}
    secrets: inherit
